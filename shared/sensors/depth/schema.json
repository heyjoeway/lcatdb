{   
    "id": "/SensorDepth",
    "type": "object",
    "properties": {
        "depth": {
            "type": "number",
            "description": "Stored in meters."
        },
        "depthRange": {
            "type": ["number", "null"],
            "description": "Max possible distance from depth. Stored in meters."
        }
    },
    "required": ["depth"]
}
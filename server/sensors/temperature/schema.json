{
    "id": "/SensorTemperature",
    "type": "object",
    "properties": {
        "temperature": {
            "type": "number",
            "description": "Stored in Celsius."
        },
        "temperatureRange": {
            "type": "number",
            "description": "Max possible distance from temperature. Stored in Celsius."
        }
    },
    "required": ["temperature"]
}
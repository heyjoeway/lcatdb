<!DOCTYPE html>
<head>
    <!--head-->
    <title><!--title--> - Remove Sensor</title>
</head>
<body>
    <!--nav-->

    <div class="container">
        <h2>Remove Sensor</h2>
        {{#canEdit}}
        <form action="./removeSensorDo" method="post" class="form-horizontal"><fieldset class="container">>
            <div class="form-group">
                <label class="col-xs-2 control-label" for="name">Configuration</label>
                <div class="col-xs-10">{{configuration._id}}</div>
            </div>
            <div class="form-group">
                <label class="col-xs-2 control-label" for="name">Sensor</label>
                <div class="col-xs-10"><input class="form-control" type="text" name="sid" value="{{query.sid}}"></div>
            </div>
            <p>Are you sure you want to remove this sensor from this configuration? The sensor will still exist, and can be re-added at any time.</p>
            <div class="form-group">
                <div class="col-xs-10 col-xs-offset-2">
                    <a href="./" class="btn btn-default">Cancel</a>
                    <button type="submit" class="btn btn-primary">Submit</button>
                </div>
            </div>
        </fieldset></form>
        {{/canEdit}}
        {{^canEdit}}
        <p>Sorry, you do not have permission to edit this configuration.</p>
        {{/canEdit}}
    </div>

    <!--scripts-->
</body>